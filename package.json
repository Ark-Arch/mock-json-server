{
  "name": "mock-json-server",
  "version": "1.0.0",
  "description": "Mock JSON server for testing APIs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["mock-api", "json-server", "testing"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
